export component MainWindow inherits Window {
	width: 400px;
	height: 150px;
	clock := Text {
		text: "05:00";
		font-weight: 600;
		color: white;

		font-size: (root.width*0.85)*0.25;
		max-width: root.width*0.85;
	}
}
